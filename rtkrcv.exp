#!/usr/bin/expect -f

set timeout -1

spawn ./rtkrcv
expect "rtkrcv>"
send -- "start\r"
expect "overwrite rover1.log       ? (y/n):"
send -- "y\r"
sleep 10
expect "rtkrcv>"
send -- "shutdown\r"
expect "shutdown rtkrcv process ? (y/n):"
send -- "y\r"
expect eof